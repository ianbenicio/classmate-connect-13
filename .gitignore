# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Test-only env files with JWTs / credentials — never commit
.env
.env.test
.env.test.local
.env.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Supabase CLI local state (generated — never commit; vaza pooler-url/project-ref)
supabase/.temp/

# Local DB binaries (ruvector / sqlite)
*.db
*.db-shm
*.db-wal

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
